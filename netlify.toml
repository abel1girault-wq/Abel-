[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  # This tells Netlify to stop the secret scanning that was blocking your build.
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
